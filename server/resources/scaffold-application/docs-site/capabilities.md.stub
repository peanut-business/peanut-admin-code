# 能力与边界

技术 Core 提供上下文、Scope、模块机制及框架接入；完整账号、组织、文件、通知和任务业务由各官方模块维护。标准应用组合提供常用管理能力，可选模块按实际依赖装配，不能把能力清单当成已完成验收。

租户、模块安装、租户开通、人员授权和数据范围各自核验。业务模块只依赖已声明的公开合同，不读写其他模块私表。商品、库存、采购等行业能力属于应用业务，不进入技术核心。

应用创建不等于开租户，包解压不等于安装完成，生成文档不等于运行已验证。按 [模块生命周期](guide/application-module-lifecycle.md) 完成实际安装、测试和升级。
