# ${MODULE_KEY} 业务与安全验收

生成器只验证目录和清单，不声明本模块的业务已实现。先定义实际输入、公开用例、权限、数据及事务，再使用应用已有的测试环境编写真实测试；不要复制空驱动或固定成功回执。

| 必须验证的行为 | 需要的实际证据 |
| --- | --- |
| tenant-a / tenant-b，forged Tenant ID | 可信上下文决定所有权；伪造输入被拒，不能覆盖身份 |
| 跨租户对象和相同局部ID | 列表、详情、计数、关联和导出不串数据 |
| 撤权 AUTHORIZATION_PERMISSION_DENIED | 公开服务和HTTP同时拒绝，旧结果不可继续访问 |
| 停用 MODULE_TENANT_DISABLED | 拒绝新业务，不误删已有数据 |
| failed migration | 失败未激活，原始失败持久化 |
| 相同失败迁移再次执行 | 未经明确修复不被 replayed，账本不重复 |
| 重复操作、文件与恢复冲突 | 以本模块声明的业务合同为准，不隐式覆盖或清理引用 |

仅限合法专属合成资源，不把其他模块的数据、主目录数据库或旧测试凭据当作默认输入。上表是待实现业务的验收合同，不是自动通过的测试。
