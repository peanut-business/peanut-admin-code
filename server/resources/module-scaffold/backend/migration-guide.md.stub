# ${MODULE_KEY} 数据迁移

此目录由模块定义中的 `backend.migrations` 登记，保存本模块的数据结构迁移。

新增迁移使用唯一、稳定的版本标识；已经执行的迁移不得因 PHP 命名空间或目录整理而重编号、覆盖或再次执行。

仅维护本模块拥有的业务表，租户业务保留租户归属。不要在服务注册、Controller 构造或普通 HTTP 请求中执行迁移。

安装和升级使用宿主的受控迁移流程。真实数据变更前需备份、预检并验证恢复方案；不要在迁移中调用真实支付、短信或任意包脚本。
